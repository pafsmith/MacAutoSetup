# LazyVim Config

This directory contains the shared Neovim setup used by this repo.

## Installation

Bootstrap the machine with this repository, then open Neovim:

```sh
nvim
```

LazyVim and plugins will be installed automatically on first start.
